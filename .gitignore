# Documentation files (excluded per user request)
*.md
*.log
*.db
*.env*
*.env.example

# Binary/Generated files
ai-scrm
.pid
tmp/
/tmp/*

# Go build artifacts
*.exe
*.test

# OS files
.DS_Store

# 大型二进制与本地数据（不入库）
# 修复(2026-09-01)：原裸规则 `server` 会匹配任意层级的 server 目录，
# 导致主服务源码 cmd/server/ 整个被 git 忽略（webhook 修复等改动无法提交）。
# 改为 /server 仅忽略根目录编译产物，cmd/server/ 源码正常入库。
/server
tessdata/
CHANGES.diff
*.part6
backups/

# ---- P-FE React 工程（构建产物与依赖不入库）----
frontend-react/node_modules/
frontend-react/dist/

# ---- 行业包密钥与打包产物（涉密/二进制）----
keys/
data/
*.aipack
build/
