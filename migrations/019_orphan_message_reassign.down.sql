-- 019 down：不逆转。回填前这些行的 conversation_id 恒为 0，但"改成了哪条会话"这一步
-- 没有留原值快照，且回填后的行已被会话维度统计消费（D2 贡献度、会话历史列表）——
-- 机械清零等于把 15+ 条真实客户消息从会话里摘出去，比不回滚更糟。
-- 需要回退治理动作时，回退代码侧的写入口护栏即可（chat_unauthorized 相似抑制分支），
-- 已回填的行保持挂接。
SELECT 1;
